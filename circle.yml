machine:
  java:
    version: 'oraclejdk8'
  node:
    version: 6.1.0
  environment:
    NODE_ENV: development
dependencies:
  override:
    - yarn install --force

test:
  override:
    - yarn run lint
    - yarn run ci-test
    - yarn run live-test
