//import ga from 'universal-analytics';


//const visitor = ga('UA-63671047-1');

const GaUtil = {
  track(type, category, action, label) {
    //debugger
    //visitor.event(type, category, action, label).send();
    //ga('send', type, category, action, label);
  }
};

export default GaUtil;
