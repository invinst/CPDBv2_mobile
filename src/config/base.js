'use strict';

// Settings configured here will be merged into the final config object.
export default {
  bottomSheet: {
    // when modifying this, remember to change $transition-duration in sass
    // style to match too
    transitionDuration: 200
  }
};
